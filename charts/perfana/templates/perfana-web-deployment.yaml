{{- if .Values.perfanaWeb.enabled -}}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: perfana-web
  labels:
    app: perfana-web
    chart: {{ include "perfana.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  replicas: {{ .Values.perfanaWeb.replicaCount }}
  selector:
    matchLabels:
      app: perfana-web
      release: {{ .Release.Name }}
  strategy:
    type: {{ .Values.perfanaWeb.deploymentStrategy }}
  {{- if ne .Values.perfanaWeb.deploymentStrategy "RollingUpdate" }}
    rollingUpdate: null
  {{- end }}
  template:
    metadata:
      labels:
        app: perfana-web
        release: {{ .Release.Name }}
        {{- with .Values.perfanaWeb.labels }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
      {{- if .Values.perfanaWeb.annotations }}
      annotations: {{ toYaml .Values.perfanaWeb.annotations | nindent 8 }}
      {{- end }}
    spec:
      {{- if .Values.perfanaWeb.serviceAccount.enabled }}
      serviceAccountName: {{ .Values.perfanaWeb.serviceAccount.name }}
      {{- end}}
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.securityContext }}
      securityContext:
      {{- toYaml . | nindent 8 }}
      {{- end }}
      containers:
        - name: perfana-web
          image: "{{ .Values.perfanaWeb.image.repository }}:{{ .Values.perfanaWeb.image.tag }}"
          imagePullPolicy: {{ .Values.perfanaWeb.image.pullPolicy }}
          {{- with .Values.perfanaWeb.command }}
          command:
          {{ toYaml . | nindent 10 }}
          {{- end }}
          {{- with .Values.perfanaWeb.args }}
          args:
          {{ toYaml . | nindent 10 }}
          {{- end }}
          {{- with .Values.containerSecurityContext }}
          securityContext:
          {{- toYaml . | nindent 12 }}
          {{- end }}
          ports:
            - name: {{ .Values.perfanaWeb.service.targetPort }}
              containerPort: {{ .Values.perfanaWeb.service.port }}
              protocol: TCP
          livenessProbe:
            failureThreshold: 8
            httpGet:
              path: /
              port: {{ .Values.perfanaWeb.service.port }}
              scheme: HTTP
            initialDelaySeconds: 30
            periodSeconds: 10
            successThreshold: 1
            timeoutSeconds: 5
          readinessProbe:
            failureThreshold: 3
            httpGet:
              path: /
              port: {{ .Values.perfanaWeb.service.port }}
              scheme: HTTP
            initialDelaySeconds: 10
            periodSeconds: 10
            successThreshold: 1
            timeoutSeconds: 5
          env:
          {{- range $key, $value := .Values.perfanaWeb.extraEnvSecrets }}
            - name: {{ $key }}
              valueFrom:
                secretKeyRef:
                  name: {{ $value.name }}
                  key: {{ $value.key }}
          {{- end }}
          {{- range $key, $value := .Values.perfanaWeb.env }}
            - name: {{ $key }}
              value: '{{ $value }}'
          {{- end }}
          resources:
{{ toYaml .Values.perfanaWeb.resources | indent 12 }}
          volumeMounts:
          {{- if .Values.perfanaWeb.volumeMounts }}
             {{- toYaml .Values.perfanaWeb.volumeMounts | nindent 10 }}
          {{- end }}
      volumes:
      {{- if .Values.perfanaWeb.volumes }}
         {{- toYaml .Values.perfanaWeb.volumes | nindent 6 }}
      {{- end }}
    {{- with .Values.perfanaWeb.nodeSelector }}
      nodeSelector:
{{ toYaml . | indent 8 }}
    {{- end }}
    {{- with .Values.perfanaWeb.topologySpreadConstraints }}
      topologySpreadConstraints:
{{ toYaml . | indent 8 }}
    {{- end }}
    {{- with .Values.perfanaWeb.affinity }}
      affinity:
{{ toYaml . | indent 8 }}
    {{- end }}
    {{- with .Values.perfanaWeb.tolerations }}
      tolerations:
{{ toYaml . | indent 8 }}
    {{- end }}
{{- end }}
