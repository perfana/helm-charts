{{- if .Values.perfanaWeb.enabled -}}
{{- if .Values.perfanaWeb.ingress.enabled -}}
{{- $ingressPath := .Values.perfanaWeb.ingress.path -}}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: perfana-web
  labels:
    app: perfana-web
    chart: {{ include "perfana.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
{{- with .Values.perfanaWeb.ingress.annotations }}
  annotations:
{{ toYaml . | nindent 4 }}
{{- end }}
spec:
  {{- if .Values.perfanaWeb.ingress.ingressClassName }}
  ingressClassName: {{ .Values.perfanaWeb.ingress.ingressClassName }}
  {{- end }}
{{- if .Values.perfanaWeb.ingress.tls }}
  tls:
  {{- range .Values.perfanaWeb.ingress.tls }}
    - hosts:
      {{- range .hosts }}
        - {{ . | quote }}
      {{- end }}
      secretName: {{ .secretName }}
  {{- end }}
{{- end }}
  rules:
    {{- range .Values.perfanaWeb.ingress.hosts }}
    - host: {{ .host | quote }}
      http:
        paths:
          {{- range .paths }}
          - path: {{ .path }}
            pathType: {{ .pathType }}
            backend:
              service:
                name: perfana-web
                port:
                  name: http
          {{- end }}
    {{- end }}
{{- end }}
{{- end }}
